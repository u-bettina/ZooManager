using System;

namespace ZooManager
{
    public class Turtle : Animal
    {
        public Turtle(string animalName, int hunger, double cost, int count) : base(animalName, hunger, cost, count)
        {
        }
    }
}
